<?php
session_start();
require_once '../config/database.php';

$product_id = $_GET['id'] ?? 0;

if ($product_id > 0) {
    $stmt = $pdo->prepare("
        SELECT p.*, 
               u.first_name as farmer_first, 
               u.last_name as farmer_last,
               u.email as farmer_email,
               u.phone as farmer_phone
        FROM products p 
        JOIN users u ON p.farmer_id = u.id 
        WHERE p.id = ?
    ");
    $stmt->execute([$product_id]);
    $product = $stmt->fetch();
    
    if ($product) {
        $is_low_stock = $product['stock'] < 10;
        $is_new = strtotime($product['created_at']) > strtotime('-7 days');
        ?>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Product Image -->
            <div class="bg-gradient-to-br from-green-50 to-gray-100 rounded-lg h-64 flex items-center justify-center relative">
                <svg class="w-24 h-24 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
                
                <?php if ($is_new): ?>
                    <span class="absolute top-3 left-3 px-3 py-1 bg-blue-500 text-white text-xs font-bold rounded-full">
                        NEW
                    </span>
                <?php endif; ?>
                
                <?php if ($is_low_stock): ?>
                    <span class="absolute top-3 right-3 px-3 py-1 bg-red-500 text-white text-xs font-bold rounded-full">
                        Only <?php echo $product['stock']; ?> left
                    </span>
                <?php endif; ?>
            </div>
            
            <!-- Product Details -->
            <div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2"><?php echo htmlspecialchars($product['product_name']); ?></h3>
                
                <?php if ($product['category']): ?>
                    <div class="mb-3">
                        <span class="px-3 py-1 bg-gray-100 text-gray-700 text-sm rounded-full">
                            <?php echo htmlspecialchars($product['category']); ?>
                        </span>
                    </div>
                <?php endif; ?>
                
                <!-- Price -->
                <div class="mb-4">
                    <span class="text-3xl font-bold text-[#10854d]">₱<?php echo number_format($product['price'], 2); ?></span>
                    <span class="ml-2 text-gray-500 text-sm">per unit</span>
                </div>
                
                <!-- Stock -->
                <div class="mb-4">
                    <span class="text-gray-600">Availability:</span>
                    <span class="font-medium <?php echo $product['stock'] > 0 ? 'text-green-600' : 'text-red-600'; ?> ml-2">
                        <?php echo $product['stock'] > 0 ? 'In Stock (' . $product['stock'] . ' units)' : 'Out of Stock'; ?>
                    </span>
                </div>
                
                <!-- Description -->
                <div class="mb-6">
                    <h4 class="font-medium text-gray-700 mb-2">Description</h4>
                    <p class="text-gray-600"><?php echo nl2br(htmlspecialchars($product['description'] ?? 'No description available')); ?></p>
                </div>
                
                <!-- Farmer Info -->
                <div class="bg-gray-50 p-4 rounded-lg mb-6">
                    <h4 class="font-medium text-gray-700 mb-2">Sold by</h4>
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center mr-3">
                            <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                            </svg>
                        </div>
                        <div>
                            <p class="font-medium text-gray-800"><?php echo htmlspecialchars($product['farmer_first'] . ' ' . $product['farmer_last']); ?></p>
                            <p class="text-sm text-gray-500">Verified Farmer</p>
                        </div>
                    </div>
                </div>
                
                <!-- Add to Cart Form -->
                <form method="POST" action="browse.php" class="mt-6">
                    <input type="hidden" name="product_id" value="<?php echo $product['id']; ?>">
                    <div class="flex items-center space-x-4">
                        <div class="flex items-center border border-gray-300 rounded-lg">
                            <button type="button" class="px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-l-lg" onclick="changeQuantity(-1)">-</button>
                            <input type="number" name="quantity" value="1" min="1" max="<?php echo $product['stock']; ?>" 
                                   class="w-16 text-center border-0 focus:ring-0" id="quantityInput">
                            <button type="button" class="px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-r-lg" onclick="changeQuantity(1)">+</button>
                        </div>
                        <button type="submit" name="add_to_cart" 
                                class="flex-1 px-6 py-3 bg-[#10854d] text-white font-medium rounded-lg hover:bg-[#0d6e40] transition-colors">
                            <i class="fas fa-cart-plus mr-2"></i> Add to Cart
                        </button>
                    </div>
                </form>
            </div>
        </div>
        <?php
    } else {
        echo '<div class="text-center p-8">
                <div class="w-16 h-16 bg-yellow-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.998-.833-2.732 0L4.346 16.5c-.77.833.192 2.5 1.732 2.5z"/>
                    </svg>
                </div>
                <h3 class="text-lg font-bold text-gray-800 mb-2">Product Not Found</h3>
                <p class="text-gray-600">The product you\'re looking for doesn\'t exist or has been removed.</p>
              </div>';
    }
} else {
    echo '<div class="text-center p-8">
            <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
            </div>
            <h3 class="text-lg font-bold text-gray-800 mb-2">Invalid Product ID</h3>
            <p class="text-gray-600">Please select a valid product.</p>
          </div>';
}
?>